# QA Guide 3.6.2:核心功能闭环优化——L4 走查指引

> 版本:v0.1 | 日期:2026-09-13
> 适用:浏览器 http://localhost:3001(前端 3001 + 后端 8000)
> 目的:为「核心功能闭环优化」30 节规格提供人工走查步骤;自动化已覆盖部分(后端 pytest 198、前端 vitest 183),本节聚焦**必须亲眼看一遍**的界面行为。
> 前置:dev server 已启动;知识库为 NovaTech synthetic demo data(synthetic: true)。

## 1. 文档详情页(/documents → 点任一文档标题)

| # | 步骤 | 预期 |
| --- | --- | --- |
| 1.1 | 点「员工手册.md」标题 | 进入详情页;基本信息含格式/状态/上传时间/分块数 |
| 1.2 | 看「内容预览」 | 显示源文件开头片段,可滚动;高度约 320px 不撑爆页面 |
| 1.3 | 看「分块内容」列表 | 每块带「Chunk 1/2/…」序号 + 标题(如「# 第一章 入职」)或首行摘要 |
| 1.4 | 点「返回文档库」 | 回到列表页 |

**失败态走查**:先在列表页上传一个空 .md(会解析失败)→ 详情页应显示「解析失败」徽标 + 失败原因行 +「暂无预览(文档解析失败或源文件缺失)」+ 空分块列表。

## 2. 来源抽屉定位(问「年假有几天?」→ 回答带引用)

| # | 步骤 | 预期 |
| --- | --- | --- |
| 2.1 | 依据条目点「查看原文」 | 抽屉滑入;引用片段在原文中**黄色高亮**(mark) |
| 2.2 | 看高亮上方提示 | 「回答引用的片段已在原文中高亮」 |
| 2.3 | 点「在文档库中查看」 | 跳到**该文档的详情页**(不再是列表页伪链接) |
| 2.4 | 校验一致性 | 抽屉原文 ⊇ 依据片段;详情页分块列表含同一 chunk |

## 3. 拒答两态(评测集 C11/C13)

| # | 步骤 | 预期 |
| --- | --- | --- |
| 3.1 | 问「公司有宠物寄养福利吗?」(无相关) | 拒答卡:「未找到答案」+「依据 0 条」 |
| 3.2 | 问「公司有健身房吗?」(信息不足) | 拒答卡变体:「找到相关信息,但不足以确定答案」+「找到相关信息 N 条,置信度不足」,**无 AI 回答正文** |

> 注:3.2 依赖知识库中存在低分相关内容;若无,视为拒答第一态,同样达标。

## 4. 会话历史「最近问过」

| # | 步骤 | 预期 |
| --- | --- | --- |
| 4.1 | 连续问 3 个不同问题 | 结果区下方出现「最近问过」列表,最近的在最前 |
| 4.2 | 点一个旧问题 | 问题文本回到输入框,结果区还原当时回答/依据,**不出现加载态、不发新请求**(快) |
| 4.3 | 对当前问题点「重新生成」 | 完成后历史条目更新为最新快照,不新增重复条目 |
| 4.4 | 问超过 6 个问题 | 列表保持 6 条,最早的被挤出 |
| 4.5 | 刷新浏览器 | 「最近问过」清空(仅当前会话,属预期) |

## 5. 文档库提示与失败恢复

| # | 步骤 | 预期 |
| --- | --- | --- |
| 5.1 | 删除一篇文档 | 出现「已删除「标题」,该文档不再参与检索」提示,约 3 秒自动消失 |
| 5.2 | 对 indexed 文档点「重建索引」 | 出现「已重建索引:标题」提示,3 秒自动消失 |
| 5.3 | 删除后立即问该文档内容 | 拒答或不再引用该文档(FR-09) |
| 5.4 | 重索引后问新内容 | 新内容可被检索(重索引真实更新) |

## 6. 处理状态与失败重试

| # | 步骤 | 预期 |
| --- | --- | --- |
| 6.1 | 上传一个正常 .md | 列表行内「上传解析中…」→ 刷新后「已索引」 |
| 6.2 | 上传空 .md | 状态「解析失败」徽标;鼠标悬停显示失败原因 |
| 6.3 | 对 failed 文档点「重建索引」 | 若源文件仍无效 → 再次 failed 并保留失败原因;若已修复 → 变 indexed |

## 7. 评测页视觉层级

| # | 步骤 | 预期 |
| --- | --- | --- |
| 7.1 | 打开评测页看一条运行历史 | 第一眼 = 策略名 + 状态徽标 + 时间 + **核心指标(宏平均/MRR 等)**;run_id/params/commit 在第二行小字(caption) |
| 7.2 | 展开看逐例明细 | 指标表格优先,参数为辅助信息 |

## 8. 回归抽查(防破坏)

| # | 步骤 | 预期 |
| --- | --- | --- |
| 8.1 | 问「年假有几天?」 | 回答 + 依据与来源 + 有用/无用反馈 + 复制(✓已复制) |
| 8.2 | 问「市内交通费每天报销上限是多少?」 | 双卡冲突呈现,不武断消除 |
| 8.3 | 四页导航(问答/评测/文档库/关于)+ 文档详情页 | 均 200,无报错;品牌蓝不变 |

---

## 自动化覆盖对照(机器侧已绿,数值来自测试运行)

- 后端 pytest **198/198**:详情端点契约、last_error 往返、索引失败标记、超大文件 400、FR-09 FTS 双路删除、拒答 relevant_hits、反馈 upsert 时间戳、reindex 全量覆盖。
- 前端 vitest **183/183**:详情页 7 例、拒答变体、抽屉定位/高亮 3 例、会话历史 4 例、删除/重建提示、列表重试、评测层级。
- 生命周期一致性 6 条 → 测试映射见 progress.md 3.6.2 节。

## 9. 文档详情返回上下文与元数据布局(2026-09-13 追加,任务 3.6.3)

### 9.1 场景 A:知识问答进入

| # | 步骤 | 预期 |
| --- | --- | --- |
| A1 | 问「年假有几天?」→ 获得回答 → 依据条目点「查看原文」 | 进入文档详情页 |
| A2 | 看左上角返回入口 | 显示「← 返回知识问答」(不是「返回文档库」) |
| A3 | 点击返回 | 回到刚才的知识问答页 |
| A4 | 核对上下文 | 问题「年假有几天?」仍在输入框;回答、依据与来源、Citation、「最近问过」都在;有反馈时选中态保留;**无加载态、不发新请求**,无需重新提问 |
| A5 | 再点浏览器 Back | 回到进入问答页之前的页面,不出现「文档详情→文档详情」重复历史 |

### 9.2 场景 B:文档库进入

| # | 步骤 | 预期 |
| --- | --- | --- |
| B1 | 文档库点任一文档标题 | 进入详情页 |
| B2 | 看返回入口 | 显示「← 返回文档库」 |
| B3 | 点击返回 | 回到文档库,列表滚动位置保持 |

### 9.3 场景 C:直接访问

| # | 步骤 | 预期 |
| --- | --- | --- |
| C1 | 新标签页直接打开 http://localhost:3001/documents/doc-hr-01 | 返回入口显示「← 返回文档库」,点击进入文档库列表页 |

### 9.4 布局与视觉

| # | 步骤 | 预期 |
| --- | --- | --- |
| L1 | 详情页顶部 | 返回入口不与浏览器顶部相贴,上方约 24–32px 留白;返回入口与标题间约 24px |
| L2 | 返回按钮 | 小字号、中性灰文字、左箭头,hover 轻微强调;**不是蓝色实心 Primary Button** |
| L3 | 四列元数据 | 文件格式/状态/上传时间/分块数四列等宽均匀,每列 Label+Value 水平居中 |
| L4 | 缩窄窗口 | 平板/移动端呈 2×2,「上传时间」不被挤压换行;数据内容不变(MD/已索引/2026-09-05/分块数) |

> 注:问答上下文保留依赖同标签页 sessionStorage(关标签页即清空,属预期);新标签页打开 ?from=qa 链接时返回入口仍显示「返回知识问答」,但走 push 兜底(无历史可回退)。

## 10. 文档库表格列对齐(2026-09-13 追加,任务 3.6.4)

| # | 步骤 | 预期 |
| --- | --- | --- |
| T1 | 打开文档库,看表头 | 「文档标题」左对齐;「格式/状态/分块数/上传时间/操作」五列表头居中 |
| T2 | 看数据行 | 标题左对齐;格式徽标(MD/TXT/PDF)、状态徽标、分块数、上传时间、操作图标均与各自表头垂直对齐(居中) |
| T3 | 上传一个文件看乐观行 | 同上对齐规则(待索引徽标/占位 — 居中) |
| T4 | 1280 / 1440 / 1920 三档窗口宽度 | 对齐不漂移;操作按钮(重建索引/删除)不挤压、不重叠、不异常换行 |
| T5 | 点「重建索引」与「删除」 | 功能不变(删除有二次确认;成功后 status 提示 3 秒消失) |

## 11. 真实 LLM(DeepSeek)链路验证(2026-09-13 追加,任务 3.6.5)

> 前置:.env 已由人写入 `LLM_PROVIDER=deepseek` 与 `DEEPSEEK_API_KEY`(Key 只在服务端,前端无读取路径);dev server 已启动。

| # | 步骤 | 预期 |
| --- | --- | --- |
| R1 | 问答页问「年假有几天?」 | 约 30–90 秒返回;回答为合成段落,正文带紫色 [1] chip(非 Mock 模板摘句);「依据与来源(1)」显示「NovaTech 请假制度」 |
| R2 | 悬停/聚焦 [1] chip | 回答引用句浅紫高亮,对应依据条目紫描边(双向联动) |
| R3 | 依据条目点「查看原文」 | 抽屉内引用片段黄色高亮;「在文档库中查看」可达详情页 |
| R4 | 问「公司今年计划收购哪家公司?」 | 拒答卡,无 AI 回答正文、不编造(找不到依据或置信度不足) |
| R5 | 看终端 dev 日志 | 首次提问后出现「LLM provider = DeepSeekProvider」,且**不含 API Key** |

**失败态走查(可选,需临时改动 .env,验证后恢复)**:

| # | 操作 | 预期 |
| --- | --- | --- |
| F1 | 暂把 `DEEPSEEK_API_KEY` 置空,提问 | 显示「回答服务未配置 LLM 密钥,暂时无法生成回答…」 |
| F2 | 改成错误 Key,提问 | 显示「LLM 服务认证失败(API Key 无效或已过期)…」 |
| F3 | 断网或改 `DEEPSEEK_BASE_URL` 为不可达地址,提问 | 显示「LLM 服务网络异常…」或「回答生成超时…」 |

> 所有失败态只显示固定话术,不会退回 Mock 回答,也不会向用户暴露服务端错误细节(细节仅在服务端日志)。

## 12. 评测矩阵视觉优化(2026-09-13 追加,任务 3.6.6)

| # | 步骤 | 预期 |
| --- | --- | --- |
| M1 | 打开评测页看矩阵表头 | 12 列表头一行不换行:检索策略 / 关键词 / 语义 / 多文档 / 相似干扰 / 术语 / 冲突 / Hit@5 / MRR / Recall@K / Precision@K / 平均延迟(长类别名已缩短,不靠缩小字体) |
| M2 | 看对齐 | 「检索策略」表头与列内容左对齐;其余 11 列表头与单元格(2/2 类计数、12/12、0.9583、「暂无数据」)全部居中,数字不再左对齐 |
| M3 | 看列宽与行高 | 「检索策略」列较宽,短指标列紧凑,平均延迟不被压缩;行高较之前舒展(py-3 档),行间有极浅灰分割线,无厚重边框/卡片阴影/彩色色块 |
| M4 | 看「混合+重排」行 | 策略名后跟小巧「默认」标签:浅蓝底 + 蓝字,无渐变无发光;**「默认」在检索策略列内**,后面的 2/2|2/2… 严格对应各自指标列;该行整体只是中性浅灰底,没有变成彩色卡片 |
| M5 | 看 Recall@K / Precision@K / 平均延迟 | 显示中性灰「暂无数据」且居中,无红色/黄色/紫色,无虚假数字 |
| M6 | 1280 / 1440 / 1920 三档窗口 | 表头不异常换行、数字列对齐不漂移、最右列不压缩、无横向溢出、行高不过高、「默认」标签不影响列宽 |
| M7 | 核对数据不变 | 矩阵数字与改版前完全一致(同一 run JSON 渲染);运行历史/逐例明细/运行评测功能不受影响 |

> 本次只动评测页视觉与 Badge 组件新增 brand 变体;评测逻辑、RAG 检索、指标计算、API 数据结构、运行历史功能零改动。

## 13. 知识问答等待状态(2026-09-14 追加,任务 3.6.7)

| # | 步骤 | 预期 |
| --- | --- | --- |
| W1 | 问「年假有几天?」,观察提问按钮 | 点击后立即变为「生成中…」:浅蓝底 + 蓝字 + 16px 细环 spinner + 禁用;连点无效(只有一次请求);不再出现灰色报错态按钮 |
| W2 | 看结果区 | 骨架卡**上方**仅一行纯文字提示「正在检索企业知识库并生成回答…」(左对齐排版、与卡片左边缘对齐,无胶囊、无图标、不居中);下方为与回答卡同结构的灰阶 Skeleton(元信息行/正文 3 行/依据与来源 2 个来源卡/操作行,卡内只有骨架屏),不再是大面积空白;Skeleton 无紫色无闪烁特效 |
| W3 | 看等待文案 | 无「首次回答约需 1 分钟」常显;无「正在检索知识库→正在重排→正在生成回答」轮播;无百分比进度条 |
| W4 | 等待超过 20 秒 | 文案变为「仍在处理中,请稍候…已等待 N 秒」(N 每秒真实递增);本会话首个问题追加「首次回答可能需要更长时间,请稍候…」;第 2 个问题起不追加 |
| W5 | 回答返回 | spinner 停止、Skeleton 消失、回答与依据正常呈现(引用 chip/查看原文/来源抽屉照常),按钮恢复「提问」;「最新」标与 v1 版本标注保留;切换无明显布局跳动;回答卡以极轻量进场动画呈现(约 240ms 淡入 + 4px 上浮,无缩放/弹性/渐变/光效) |
| W6 | 制造失败(断网或停掉 8000 后端再问) | 显示「回答失败」错误卡 + 文案(如「RAG 服务不可达,请稍后重试」)+「重试」按钮;绝不继续显示「生成中…」;与「知识库中未找到答案」拒答卡严格区分 |
| W7 | 恢复后点「重试」 | 重新进入生成中态并正常返回回答 |
| W8 | 1280 / 1440 / 1920 与窄窗口 | Skeleton 不撑破 960px 容器、无横向溢出、按钮不溢出;重新生成(卡内加载)行为不变 |
| W9 | 回答进场动画细节(2026-09-14 精修) | 提问 → 回答返回瞬间:仅 240ms 一次性 opacity 0→1 + translateY 4px→0;**无缩放、无弹性、无渐变、无明显 AI 特效**;骨架与回答卡同结构,高度不跳、无 Layout Shift;完整 Loading → Success 切换无闪烁、无跳动、无重复 Skeleton |
| W10 | 不触发进场的场景(2026-09-14 精修) | ①点「最近问过」历史条目回看:回答卡直接呈现,不播进场动画;②重新生成完成:新卡直接出现(旧卡全程可见,无 Skeleton 切换);③刷新页面恢复快照:不播动画 |

> 本次只动等待状态呈现与 Button loading 变体;RAG 检索、LLM 调用、引用机制、拒答/冲突/评测/文档库逻辑零改动。

## 14. 文档库上传区域优化(2026-09-14 追加,任务 3.6.9)

> 本次只动 UploadZone 组件呈现与文档库页错误文案;上传/解析/索引/删除/重建索引逻辑零改动。

| # | 步骤 | 预期 |
| --- | --- | --- |
| U1 | 打开文档库页看上传区(Default) | 中性浅灰虚线框 + 白底,内容垂直居中,自上而下仅三行:线性 Upload 图标(20px,与标题协调)→ 主标题「拖入或选择文档」(字号字重明显高于其他文字)→「支持 .md / .pdf / .docx / .html / .txt」(扩展名等宽字体)→ 更小更浅的「不支持扫描件 OCR,请先将扫描件转换为文本」;**无**「点击选择文件…」操作提示行(2026-09-14 回调删除);无紫色、无发光、无渐变装饰 |
| U2 | 鼠标悬停上传区(Hover) | 虚线边框轻变品牌蓝 + 非常浅蓝底 + 图标轻染蓝(约 150ms 色彩过渡);无动画特效、无发光 |
| U3 | 拖一个 .md 文件悬停在上传区(Drag Over) | 边框变品牌蓝实线 + 浅蓝底,支持格式行原位换为「松开鼠标以上传」(不增行、无布局跳动);移出后支持格式行恢复 |
| U4 | 拖一个 .png 文件松手(Unsupported File) | 边框变 danger 红 + 浅红底,卡内出现「文件格式不支持,请选择 .md / .pdf / .docx / .html / .txt 文件」(错误就在操作区附近,role=alert);文档列表不出现该文件、不发上传请求 |
| U5 | 选一个 .md 文件上传(Uploading) | 标题换「正在上传 {文件名}…」+ 支持格式行原位换「上传中,请稍候…」;上传区不可点、不可拖入,顶部「上传文档」按钮禁用;无假进度条;表格乐观行「上传解析中…」为真实进度 |
| U6 | 三个入口通道 | ①点击上传区任意位置打开文件选择;②Tab 聚焦上传区出现清晰焦点环,Enter/Space 打开文件选择;③文件拖入松手正常触发上传 |
| U7 | 布局稳定性 | 卡片整体高约 160px(回调后明显更矮);文档库页头/表格/其他区域布局不变;容器宽度仍为 1152px |
| U8 | 1280 / 1440 / 1920 三档窗口 | 上传区随容器宽度正常拉伸,文案不换行错位;375px 无横向溢出 |

> Hover / Drag Over 视觉为 CSS-only 类名切换(组件测试已断言类名),真实浏览器观感与拖拽进出闪烁(深度计数防抖)需人工确认。

## 走查结论落档

走查完成后在 progress.md 3.6.2 节尾部追加一行结论(全部通过 / 列出遗留),并随 DesignRules 走查记录归档。

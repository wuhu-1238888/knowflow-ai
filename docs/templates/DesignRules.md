# 设计规则(Design Rules)

> 版本:v0.1 | 最后更新:YYYY-MM-DD
> 对应阶段:Stage 09 设计系统

<!-- 本文件定义「必须做 / 禁止做」;DesignSystem.md 定义「系统是什么」。两者分工,铁律:需要新值先改 DesignSystem.md 再使用。 -->

## UI 原则(2–3 条)

<!-- 如:「专业克制」「AI 内容有明确视觉标识」。 -->

## 组件使用规则

<!-- 每个核心组件:必须用哪个、禁止用哪个、什么场景用什么变体。 -->

## 逐页页面规则

<!-- 每个页面 = 目标 / 必备区块 / 页面专属禁令。 -->

## 禁止事项

<!-- 必须是可 grep 检查的硬约束,如:
- 禁止硬编码色值(检查命令:`grep -rn "#[0-9a-fA-F]\{3,6\}" src/`)
- 禁止新增未登记的组件变体 -->

## 提交前自检清单

1. [ ] {grep 检查无硬编码偏离值}
2. [ ] {新 UI 组合已有组件,无重复造轮子}
3. [ ] …
<!-- 固定 5–12 条,每条可执行。 -->

## 走查记录

<!-- 每次页面走查结论追加于此;{日期} {页面} {结论}。 -->

# 产品架构(Product Architecture)

> 版本:v0.1 | 最后更新:YYYY-MM-DD
> 对应阶段:Stage 06 产品架构

## 信息架构

<!-- 导航 / 页面树(ASCII 图,不用图片——保持可 grep、可 diff)。 -->

## 模块划分

| 模块 | 职责 | 消费哪些模块的数据 | 提供哪些数据 |
| --- | --- | --- | --- |

<!-- 必须显式标出【核心数据源模块】:所有下游模块都消费它,它必须最先做(纵向核心链路阶段的对象)。 -->

## 数据流与联动关系

```
{核心数据源} → {下游模块 A}
             → {下游模块 B} → {联动场景}
```

<!-- 有向无环;发现循环依赖 = 架构错误,回改模块划分。 -->

## FR 覆盖核对

<!-- 每个 P0 FR 必须能落到某个模块;落不了的 = PRD 或架构有缺口。 -->

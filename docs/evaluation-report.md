# 三模式对比评测报告(任务 3.4.2)

> 版本:v1.0 | 2026-09-10
> 诚信规则(evaluation-plan §4):所有数值只来自 run JSON,**每格标注 run-id,禁止手填/预估**;
> 阈值不变更(检索层 Hit@5 ≥ 11/12、MRR ≥ 0.8,生成层各线见第 3 节)。
> 核对方式:人逐格打开对应 run JSON(docs/eval-results/)比对数字。

## 1. 实验环境与参数冻结

| 项 | 值 |
| --- | --- |
| params_hash | `365740ee216243e4`(分块/检索常量 + 模型架构与权重体积,compute_params_hash) |
| 检索常量 | TOP_K=8 / CANDIDATE_FACTOR=3 / RRF_K=60(改动须人拍板 + 重跑评测) |
| 模型 | bge-m3(embedding)、bge-reranker-v2-m3(rerank,use_fp16=False,CPU 推理) |
| 拒答阈值 τ | TAU_RERANK=0.30 / TAU_VECTOR=0.58(Stage 10 人拍板;校准依据 docs/eval-results/tau-calibration.md) |
| 文档集 | docs/demo-data/documents 20 篇(自 d2c8959 起未变,已核实 git 历史) |
| 评测集 | cases.yaml 14 例(1e8b0aa 人工标注签字,此后未变) |

**检索层两个锚点批次**(同 params_hash、同文档集,代码差异仅 §2.4 截断实验及其回退):

| 批次 | doc_commit | 说明 |
| --- | --- | --- |
| 164508Z | `9ac3f84` | 基线批次(3.3.4 完成后) |
| 121215Z | `18a09ef` | 当前锚点批次(截断实验回退后复跑;与基线逐格一致 → 可复现性 FR-08 成立) |

## 2. 检索层(12 例有答案样例;无答案 2 例不参与,plan §1)

### 2.1 指标总表(每格 run-id)

| 模式 | Hit@5 | MRR | 基线 run-id(164508Z) | 复跑 run-id(121215Z) |
| --- | --- | --- | --- | --- |
| vector(对照) | 12/12 | 0.9583 | run-f7c5f6b48f37 | run-0cf561b16349 |
| hybrid(对照) | 12/12 | 0.9028 | run-5a7748a6acd1 | run-e66ce63c0554 |
| hybrid_rerank | 12/12 | **0.9583** | run-1210ff838c34 | run-24c53e2a5f0a |

**达标判定**(hybrid_rerank,plan §2 阈值):Hit@5 = 12/12 ≥ 11/12 ✓;MRR = 0.9583 ≥ 0.8 ✓。
vector / hybrid 为对照组,只记录数值展示增益、不设达标线。

### 2.2 Δ 增益(数字取自 §2.1 各 run JSON)

| 对比段 | ΔHit@5 | ΔMRR | 说明 |
| --- | --- | --- | --- |
| Vector → Hybrid | 0 | **-0.0555**(0.9028 − 0.9583) | **负增益,如实呈现**:逐例排名 C05 1→2、C10 2→3,其余不变——RRF 融合在 12 例上未带来 MRR 提升 |
| Hybrid → Hybrid+Rerank | 0 | **+0.0555**(0.9583 − 0.9028) | 逐例排名 C05 2→1 恢复、C10 3→2(回到与 vector 一致的水平) |

结论(诚实口径):Hit@5 三模式均 12/12,Δ = 0;MRR 增益只存在于「Hybrid → Rerank」段,其作用是
**抵消 RRF 融合带来的排序噪声**(把 hybrid 的 0.9028 拉回 vector 基线 0.9583),同时 rerank_score
为拒答阈值提供分离度最大的口径(τ 校准依据 tau-calibration.md)。

### 2.3 逐例排名对照(164508Z;121215Z 逐例一致,已核对)

| case | vector | hybrid | hybrid_rerank |
| --- | --- | --- | --- |
| C01 | 1 | 1 | 1 |
| C02 | 1 | 1 | 1 |
| C03 | 1 | 1 | 1 |
| C04 | 1 | 1 | 1 |
| C05 | 1 | 2 | 1 |
| C06 | 1 | 1 | 1 |
| C07 | 1 | 1 | 1 |
| C08 | 1 | 1 | 1 |
| C09 | 1 | 1 | 1 |
| C10 | 2 | 3 | 2 |
| C11 / C12 | —(无答案类,不参与命中指标) | — | — |
| C13 | 1 | 1 | 1 |
| C14 | 1 | 1 | 1 |

> 单元格数字 = rank_of_first_expected(首个期望 doc 的 1-based 排名),来自各 run JSON 的 per_case。

### 2.4 性能 NFR 实测(诚实记录:检索 < 3s 不达标)

- **检索接口耗时**(2026-09-10 实测,截断回退后代码,curl 计时):hybrid_rerank 热态每问
  35.4 / 31.0 / 30.9 s,拒答一问 30.7 s。耗时构成:reranker CPU 推理 24 个候选**全文本**(~28s)+
  embedding / BM25(~1–2s)+ Mock LLM(≈0s,确定性摘句)。NFR「检索接口 < 3s」**超标约 10 倍,不达标**。
- **截断优化实验(负结果,已回退)**:将 rerank 输入截断到 200 字,热态每问降至 ~16s,但
  MRR 0.9583 → **0.8542**(run-eece000b19c7,115419Z 批次:逐例 C04 排名 1→4、C08 1→2,该两例的
  区分信息位于 chunk 300 字之后)。探测表明能保住指标的 500 字截断相对全文本仅快 ~10%,
  无实际收益 → 人拍板回退(commit `18a09ef`),检索性能优化另立任务
  (候选数 CANDIDATE_FACTOR 3→2 / ONNX 加速等,均须人拍板 + 重跑评测)。
  > 参数冻结盲点(如实记录):截断实验批次(115419Z)与基线同 params_hash——当时截断常量未纳入
  > params_hash 载荷;该常量已随回退删除,当前代码无此盲点。
- **评测全量时长**:121215Z 批次 12:12:15Z 启动 → 三模式 run JSON 落盘完成 20:20:17(+08:00),
  实测 **8 分 2 秒 < 10 分钟 NFR** ✓(余量约 2 分钟,主要耗时在 hybrid_rerank 单模式 ~7.3 分钟)。
- **生成接口 NFR(< 15s)**:DeepSeek 单次调用实测 2.17–2.63 s(均值 2.33 s),**达标** ✓(§3.2)。

## 3. 生成层(14 例全量;人工判定为金标准,plan §2)

### 3.1 Mock 结构事实(gen-a761359f66d1,核对表 review-2026-09-09T064633Z.md)

> 该核对表 doc_commit = 6190456;此后 documents/ 与 cases.yaml 无任何改动(已核实 git 历史),
> 检索侧输入仍有效。**人工五列尚未填写**(L5 走查待人完成)。

| 线 | Mock 实测(机器可判部分) | 达标判定 |
| --- | --- | --- |
| 拒答 | C11(confidence 0.0015)/ C12(0.0035)均拒答;12 例有答案样例 0 误拒 | 结构达标;人工复核待完成 |
| 引用 | 12 例回答全部 3 条引用,编号 / quote 由管线规则侧重建(必为原文子串) | 结构达标;人工逐条核对待完成 |
| 要点 | 机器预检(子串命中,必要不充分)5/33 | Mock 摘句机制下无判定意义,**真实验收待 Key** |
| 幻觉 | Mock 逐字摘录 → 结构保证,非真实生成检验 | **真实验收待 Key** |
| 冲突 | Mock conflicts 恒 None → 冲突 2 例(C13/C14)必 ✗ | **真实验收待 Key** |

逐例 Mock 预检(hybrid_rerank 表,核对重点):

| case | 实际 | 要点预检 | 引用 | 冲突 | confidence |
| --- | --- | --- | --- | --- | --- |
| C01 | 回答 | 2/2 | 3 | 0 | 0.833589 |
| C02 | 回答 | 1/2 | 3 | 0 | 0.985044 |
| C03 | 回答 | 0/1 | 3 | 0 | 0.845777 |
| C04 | 回答 | 0/3 | 3 | 0 | 0.756669 |
| C05 | 回答 | 0/4 | 3 | 0 | 0.987686 |
| C06 | 回答 | 1/4 | 3 | 0 | 0.993064 |
| C07 | 回答 | 0/3 | 3 | 0 | 0.996074 |
| C08 | 回答 | 0/3 | 3 | 0 | 0.659162 |
| C09 | 回答 | 0/3 | 3 | 0 | 0.990196 |
| C10 | 回答 | 1/2 | 3 | 0 | 0.985778 |
| C11 | 拒答 | 0/0 | 0 | 0 | 0.001534 |
| C12 | 拒答 | 0/0 | 0 | 0 | 0.003534 |
| C13 | 回答 | 0/3 | 3 | 0 | 0.99304 |
| C14 | 回答 | 0/3 | 3 | 0 | 0.99269 |

### 3.2 真实验收(遗留 #1 代码侧解除,2026-09-10 完成)

Key 由人写入 `.env`(DEEPSEEK_API_KEY + LLM_PROVIDER=deepseek,AI 绝不写 Key),真实
DeepSeek 重跑三模式 × 14 例,最终批次 **132817Z**,核对表 `review-2026-09-10T132817Z.md`
(重点核对 hybrid_rerank 表)。三模式 run-id:

| 模式 | run-id | provider | doc_commit | skipped |
| --- | --- | --- | --- | --- |
| vector | gen-ed1ff99a8dcd | DeepSeekProvider | 951c553 | 0 |
| hybrid | gen-b3f6ddcdc2fd | DeepSeekProvider | 951c553 | 0 |
| hybrid_rerank | gen-99ccfb6c4d33 | DeepSeekProvider | 951c553 | 0 |

**机器可判事实(全部来自 run JSON,人工判定为金标准)**:

- **拒答**:C11/C12 三模式均拒答(2/2),12 例有答案样例 0 误拒 ✓。
- **引用**:12 例回答全部 ≥1 条引用;正文 `[n]` 标记与引用编号**三模式 0 错配**
  (程序化核对:每例正文标记集合 ≡ 引用 index 集合)。
- **要点**:机器预检(子串命中,必要不充分)三模式均 11/33——真实生成不逐字摘录,
  预检覆盖率远低于 Mock,人工判定为准。
- **冲突**:结构化 conflicts 三模式均 0 例。C13/C14 答案文本均**并列呈现两个版本、不选边**
  并引用双方文档(散文式冲突呈现),但模型未输出结构化 conflicts 字段——
  **达标与否待人拍板**(见 §5 遗留 #1 判定项)。
- **生成接口 NFR(< 15s)**:DeepSeek 单次调用实测 2.17 / 2.20 / 2.63 s(均值 2.33 s)→
  **达标** ✓(2026-09-10 实测,同一 Key/网络)。

**过程如实记录(三次重跑的引用编号缺陷与修复)**:

1. 第一次真实重跑(125001Z):3 处正文标记与引用编号错位(C02/C09 hybrid、C07 hybrid_rerank
   的 `[1,3]` vs `[1,2]`)——根因:管线层把引用稠密重编号,正文仍保留模型原始块编号。
2. 修复一:`_renumber_markers` 同步重写正文标记(悬空标记删除,占位符两段式替换防级联)。
3. 第二次重跑(130725Z):错配反而增至 7/7/9——复现定位:模型对同一块给出多条摘句
   (如 C05 块 1 给 9 条 citation),`ref_map` 被逐条覆盖(`ref_map[1]` 被覆盖到 9),
   正文 `[1]→[9]`、`[2]→[10]`、`[3]→[11]` 全部悬空。
4. 修复二:`_to_draft` 对重复 ref 仅取首个(管线层本就按 chunk 去重);回归测试入档。
5. 第三次重跑(132817Z):三模式 0 错配(上表)。两次修复均有单测覆盖,后端 pytest 181/181。

### 3.3 五线人工判定待办(132817Z 核对表)

- 逐例填 ✓/✗/—(要点 / 幻觉 / 引用 / 拒答 / 冲突五列),重点 = hybrid_rerank 表;
- 达标线:要点 ≥90% / 幻觉 0 例 / 引用错误 = 失败例 / 拒答 2/2 且 0 误拒 / 冲突 2/2;
- C13/C14 冲突线判定口径先拍板:结构化 conflicts None 但散文并列呈现是否判 ✓(§5)。

## 4. 达标汇总

| 验收项 | 结果 | 依据 |
| --- | --- | --- |
| 检索层 Hit@5(hybrid_rerank) | **达标** 12/12 ≥ 11/12 | run-24c53e2a5f0a |
| 检索层 MRR(hybrid_rerank) | **达标** 0.9583 ≥ 0.8 | run-24c53e2a5f0a |
| 可复现性(同参数重跑一致) | **成立**:121215Z 与 164508Z 三模式逐格一致 | §2.1 |
| 评测全量时长 NFR(< 10 分钟) | **达标** 8 分 2 秒 | run JSON 落盘时间(§2.4) |
| 检索接口 NFR(< 3s) | **不达标**(热态 ~31s/问,超标约 10 倍) | §2.4 实测 |
| 生成接口 NFR(< 15s) | **达标** 单次调用 2.17–2.63s(均值 2.33s) | §3.2 实测 |
| 引用编号一致性(正文 ↔ 引用) | **达标** 三模式 0 错配 | gen-ed1ff99a8dcd 等三 run JSON |
| 拒答行为(机器判) | **达标** 2/2 拒答且 0 误拒 | §3.2 三 run JSON |
| 生成层五线 | **待人工判定**(132817Z 核对表五列待填;冲突线 C13/C14 口径先拍板) | review-132817Z |

## 5. 遗留与后续

- **遗留 #1(代码侧解除,判定侧待人工)**:DeepSeek 真实重跑完成(§3.2,132817Z 批次);
  剩余 = 五线人工判定(要点 ≥90% / 幻觉 0 / 引用错误 = 失败例 / 拒答 2/2 且 0 误拒 /
  冲突 2/2)+ C13/C14 冲突线口径拍板:**模型以散文并列呈现两版本并引用双方文档
  (逐例输出可见),但结构化 conflicts 字段为 None**——判 ✓(散文呈现达标)/ 判 ✗
  (要求结构化输出,则需提示词强化或规则侧检测,改管线后重跑生成层)。
- **检索性能优化(新任务)**:NFR「检索 < 3s」不达标;截断方案已证伪(§2.4),候选方向
  CANDIDATE_FACTOR 3→2、ONNX 加速、半精度等,均须人拍板 + 重跑评测。
- **L5 人工走查**:132817Z 核对表人工五列待填(§3.3)。

# KnowFlow AI 环境配置(复制为 .env 使用;.env 不入库)
# LLM Provider:mock 为默认(零 Key 跑通全链路与评测);真实 provider 首选 deepseek,备选智谱
LLM_PROVIDER=mock
DEEPSEEK_API_KEY=
# 本地模型目录(首次运行自动下载 bge-m3 / bge-reranker-v2-m3 至此,体积数 GB,不入库)
MODEL_DIR=runtime/models
